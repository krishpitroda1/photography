import React from "react";
import "./SplashScreen.css"; // Ensure correct CSS import

const SplashScreen = () => {
  return (
    <div className="splash-container">
      <h1 className="splash-title">WEDDING PHOTOGRAPHY</h1>
      <h2 className="splash-subtitle">Redefined</h2>
    </div>
  );
};

export default SplashScreen;
